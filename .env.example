# Hyperliquid credentials
HL_SECRET_KEY=your_private_key_here
HL_WALLET_ADDRESS=your_wallet_address_here

# Network: false = mainnet (real prices), true = testnet
# Paper trading is safe on mainnet — it only reads prices, never places orders
HL_TESTNET=false
